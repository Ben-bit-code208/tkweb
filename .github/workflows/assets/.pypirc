[distutils]
  index-servers =
    pypi
    tkweb

[pypi]
  username = __token__
  password = ${{ secrets.PYPI_API_TOKEN }}
[tkweb]
  repository = https://upload.pypi.org/legacy/
  username = __token__
  password = ${{ secrets.PYPI_API_TOKEN }}